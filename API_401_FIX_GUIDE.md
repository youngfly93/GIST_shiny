# AI API 401错误修复指南

## 问题诊断

你遇到的401错误表明API密钥认证失败。现在系统已经改进了错误处理，会提供更详细的诊断信息。

## 快速解决方案

### 1. 运行诊断工具
```r
# 在GIST_shiny目录下运行
source("diagnose_api.R")
```

这个脚本会：
- 检查.env文件是否存在
- 验证API密钥格式
- 测试API连接
- 提供具体的错误诊断

### 2. 检查API密钥状态

1. **访问 OpenRouter**：https://openrouter.ai
2. **登录你的账户**
3. **检查以下内容**：
   - API密钥是否有效
   - 账户余额是否充足
   - 密钥权限是否正确

### 3. 更新API密钥

如果密钥有问题：

1. **生成新密钥**：
   - 在OpenRouter dashboard中生成新的API密钥
   - 复制完整的密钥（格式：`sk-or-v1-...`）

2. **更新配置**：
   ```bash
   # 编辑.env文件
   OPENROUTER_API_KEY=sk-or-v1-你的新密钥
   ```

3. **重启应用**：
   ```r
   # 重启Shiny应用
   source("start_ai.R")
   ```

## 改进的错误处理

现在当API认证失败时，系统会显示：

```
🔑 API密钥认证失败 (401错误)

**可能的原因：**
1. API密钥已过期或无效
2. API密钥格式不正确
3. 账户余额不足

**解决方案：**
1. 检查 .env 文件中的 OPENROUTER_API_KEY
2. 登录 https://openrouter.ai 检查密钥状态
3. 生成新的API密钥并更新配置
4. 确认账户有足够的余额

请联系管理员检查API配置。
```

## 测试修复效果

修复后，尝试生成一个图表并查看AI分析：

1. 选择任一模块（如Gender Analysis）
2. 输入基因名（如TP53）
3. 点击"更新"生成图表
4. 查看AI聊天窗口是否显示正确的分析结果

## 如果问题仍然存在

1. **检查网络连接**
2. **确认.env文件在正确位置**
3. **验证API密钥完整性**
4. **联系OpenRouter支持**

## 联系信息

如果需要进一步帮助，请：
- 运行诊断工具获取详细信息
- 检查控制台日志中的错误信息
- 确认API账户状态

---

*该修复指南解决了401认证错误并改进了错误诊断功能。*